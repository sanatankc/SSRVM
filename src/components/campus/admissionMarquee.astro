---
import MarqueeContent from './marqueeContent.astro'

---

<div class="marquee py-4 relative" style={{ background: 'linear-gradient(249.28deg, #FBFFF7 0%, #F6FFED 0%, #FFFAF7 100%)' }}>
  <ul class="marquee__content items-center">
   <MarqueeContent />
   <MarqueeContent />
   <MarqueeContent />
  </ul>
  <!-- Mirrors the content above -->
  <ul class="marquee__content items-center" aria-hidden="true">
    <MarqueeContent />
    <MarqueeContent />
    <MarqueeContent />
  </ul>
</div>


<style>
.marquee {
  --gap: 1rem;
  display: flex;
  overflow: hidden;
  user-select: none;
  /* gap: 2rem; */
}

.marquee__content {
  flex-shrink: 0;
  display: flex;
  /* justify-content: space-around; */
  min-width: 100%;
  gap: var(--gap);
  animation: scroll 20s linear infinite reverse;
}

@keyframes scroll {
  from {
    transform: translateX(0);
    /* opacity: 1; */
  }
  to {
    transform: translateX(calc(-100% - var(--gap)));
    /* opacity: 0; */
  }
}

</style>